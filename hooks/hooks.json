{
  "description": "Sales-Recon v3 session startup — checks API keys and shows available commands",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/session-briefing.sh",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
